@{
    ViewData["Title"] = "Acceso denegado";
    Layout = "_Layout";
}

<div class="text-center py-5">
    <h1 class="h4 text-danger mb-3">Acceso denegado</h1>
    <p class="text-muted">Tu nivel de usuario no tiene permiso para acceder a esta sección.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-rmp">Volver al inicio</a>
</div>
